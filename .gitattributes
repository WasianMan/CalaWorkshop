# Normalize to LF in the repo so shell scripts, Dockerfiles, and CI behave on Linux.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.ps1 text eol=crlf

# Binary artifacts.
*.zip binary
*.png binary
*.webp binary
*.svg binary
